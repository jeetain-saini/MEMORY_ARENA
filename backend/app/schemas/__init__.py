"""Schemas - Pydantic request/response models (the public wire contract).

Kept separate from domain entities so the external API and internal model
evolve independently. Stage 0: structure only - no implementation.
"""