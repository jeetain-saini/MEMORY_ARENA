"""Observability services (Stage 13).

Deterministic, framework-free observability helpers: the controllable
``FrozenClock`` and the memory-health metrics service. Pure application logic
that depends only on ports — adapters live in ``infrastructure/observability``.
"""
