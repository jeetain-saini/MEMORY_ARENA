"""Services - application/domain services.

Logic that legitimately spans more than one entity (e.g. consolidating a
memory cluster, blending vector + graph retrieval results).
Stage 0: structure only - no implementation.
"""